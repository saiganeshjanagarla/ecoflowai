services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ecoflow
      POSTGRES_USER: ecoflow
      POSTGRES_PASSWORD: ecoflow
    ports:
      - "5432:5432"
    volumes:
      - ecoflow-postgres:/var/lib/postgresql/data
volumes:
  ecoflow-postgres: